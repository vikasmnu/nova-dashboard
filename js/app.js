/* ==========================================
   NOVA DASHBOARD
   App
========================================== */

document.addEventListener("DOMContentLoaded", () => {

    console.log(`${CONFIG.APP.NAME} v${CONFIG.APP.VERSION}`);

    Wallpaper.init();

    Time.init();

});